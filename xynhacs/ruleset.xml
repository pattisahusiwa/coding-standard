<?xml version="1.0"?>
<ruleset name="xynhacs">
    <description>PHP standard for all Xynha projects</description>

    <arg name="tab-width" value="4"/>

    <rule ref="PSR2"/>

    <!--
    Fix wrong indentation, eq:
      $a = 1;
          $b = 2;

      These lines should be like this
      $a = 1;
      $b = 2;
    -->
    <rule ref="Generic.WhiteSpace.ScopeIndent">
        <properties>
            <property name="indent" value="4"/>
            <property name="exact" value="true" />
            <property name="tabIndent" value="false" />
            <property name="ignoreIndentationTokens" type="array" value=""/>
        </properties>
    </rule>

    <!-- Class properties spacing -->
    <rule ref="Squiz.WhiteSpace.MemberVarSpacing"/>

    <!-- Functions spacing -->
    <rule ref="Squiz.WhiteSpace.FunctionSpacing">
        <properties>
            <property name="spacing" value="1" />
            <property name="spacingAfterLast" value="0" />
        </properties>
    </rule>

    <!--
    Ensure a space before and after an operator.
    However, this sniff allows many space before operator.
    -->
    <rule ref="Squiz.WhiteSpace.OperatorSpacing"/>

    <!-- Arrays -->
    <rule ref="Squiz.Arrays.ArrayDeclaration">
        <exclude name="Squiz.Arrays.ArrayDeclaration.NoComma"/>

        <!-- Has been handled by Squiz.WhiteSpace.OperatorSpacing -->
        <exclude name="Squiz.Arrays.ArrayDeclaration.SpaceAfterDoubleArrow"/>
    </rule>
    <rule ref="Squiz.Arrays.ArrayBracketSpacing"/>
</ruleset>
